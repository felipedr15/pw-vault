# Copy this file to .dev.vars for local worker development
# These values override [vars] in wrangler.toml during `wrangler dev`

# Restrict CORS to a specific origin (use * for local dev)
ALLOWED_ORIGIN=http://localhost:5173
